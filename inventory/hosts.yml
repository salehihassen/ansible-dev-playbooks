---
# Inventory configuration
# Location: inventory/hosts.yml

# Define your hosts and groups here
all:
  children:
    dev_machines:
      hosts:
        # Add your remote machines below
        headbutt:

        # devbox02:
        #   ansible_host: 192.168.1.101
        #   ansible_user: user
      vars:
        # Group-level variables for all dev machines
        ansible_python_interpreter: /usr/bin/python3
